module.exports = {
  plugins: {
    // These plugins are applied to your CSS
    tailwindcss: {},   // The Tailwind CSS plugin itself
    autoprefixer: {},  // Automatically adds vendor prefixes for better browser compatibility
  },
};
